apiVersion: v1
kind: Service
metadata:
  name: {{ include "php-backend.fullname" . }}
  labels:
    {{- include "php-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "php-backend.selectorLabels" . | nindent 4 }}
