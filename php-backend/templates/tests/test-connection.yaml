apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "php-backend.fullname" . }}-test-connection"
  labels:
    {{- include "php-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "php-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
